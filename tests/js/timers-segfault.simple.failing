/**
 * @file        timers-segfault.simlpe
 *              MRTC for a segfault when using the pmjs-lib/event-timers.js module.
 * @author      Wes Garland, wes@distributive.network
 * @date        July 2023
 *
 * timeout: 10
 */

setTimeout(() => { /* a.push(1) */ 1 });
const interval = setInterval(() => { 1 }, 500);
setTimeout(() => { 2 }, 1000);
